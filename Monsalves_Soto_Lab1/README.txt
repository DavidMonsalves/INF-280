Es necesario instalar la librería "numpy", "pandas", y "matplotlib", las cuales se pueden instalar mediante el comando:

>	pip install numpy
>	pip install matplotlib
>	pip install pandas

Cada respuesta está marcada por la sección código y la sección respuesta. En ciertas preguntas se prefirió ordenar los resultados en tablas para una mejor visualización

Precaución: Si ejecuta una celda, las probabilidades podrían cambiar.